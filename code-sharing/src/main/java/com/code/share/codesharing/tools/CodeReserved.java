package com.code.share.codesharing.tools;

public class CodeReserved {


    /*resAccounts.forEach(stringObjectMap -> {
        stringObjectMap.remove("CREATE_DATE");
    });*/

    /*List<Map> newREs = new ArrayList<>();
        for(Map m: res){

            String particular_asset = "particular_asset";
            String parent_id = "parent_id";

            Map map = new HashMap();
            map.put(particular_asset, m.get(particular_asset).toString());
            map.put(parent_id, m.get(parent_id).toString());

            newREs.add(map);
        }*/



        /*res.stream().forEach(map -> {
            map.get("req_endorsed_by").toString();
        });*/

        /* List<Map> groupB = Collections.singletonList(res.stream().collect(Collectors.groupingBy(map -> map.get("id_parent"),
                 Collectors.mapping(map -> map.get("particular_asset").toString(), Collectors.toList())
         )));*/
}
